<h1>Edit user</h1>

<%= form_for(@user) do |f| %>
   <%= render 'fields', :f => f %>

  <div class="actions">
    <td><%= f.submit "Update" %></td>
  </div>
</tr>
</table>
<% end %>

<table>
  <div>
    <tr>
      <td><%= gravatar_for @user %><a href="http://gravatar.com/emails" target="_blank" >Change picture</a></td>
    </tr>
  </div>
</table>
